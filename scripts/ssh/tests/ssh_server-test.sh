#!/bin/bash

# Set the "PasswordAuthentification" to 'yes' in the /etc/ssh/sshd_config file.

# After that the client registered his public RSA key, set the
# "PasswordAuthentification" to 'no' in the /etc/ssh/sshd_config file.
